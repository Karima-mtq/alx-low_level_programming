#include <stdio.h>

int main() {
    char character;

    character = 'H';
    printf("The ASCII value of %c is %d\n", character, character);

    character = 'e';
    printf("The ASCII value of %c is %d\n", character, character);

    character = 'l';
    printf("The ASCII value of %c is %d\n", character, character);

    character = 'l';
    printf("The ASCII value of %c is %d\n", character, character);

    character = 'o';
    printf("The ASCII value of %c is %d\n", character, character);

    character = 'W';
    printf("The ASCII value of %c is %d\n", character, character);

    character = 'o';
    printf("The ASCII value of %c is %d\n", character, character);

    character = 'r';
    printf("The ASCII value of %c is %d\n", character, character);

    character = 'l';
    printf("The ASCII value of %c is %d\n", character, character);

    character = 'd';
    printf("The ASCII value of %c is %d\n", character, character);

    character = '!';
    printf("The ASCII value of %c is %d\n", character, character);

    return 0;
}

