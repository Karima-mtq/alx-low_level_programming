#include "main.h"

/**
 * mul - Function checks if a character is a digit
 * @a: the first integr to be multiplied
 * @b: the second integer to be multiplied
 * Return: success 1 otherwise 0
 */

int mul(int a, int b)
{
	int m = a * b;

	return (m);
}
