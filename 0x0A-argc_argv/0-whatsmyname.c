#include <stdlib.h>
#include <stdio.h>
#include "main.h"
/**
 * main - function
 * @argc: argument
 * @argv: list
 * Return: 0
 */
int main(int argc, char *argv[])
{
	if (argc > 0)
	{
		printf("%s\n", argv[0]);
	}
	return (0);
}
